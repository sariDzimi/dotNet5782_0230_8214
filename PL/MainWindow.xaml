<Window x:Class="PL.MainWindow"
        x:Name="OurProject"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="commonStyle"
           TargetType="Control">
            <Setter Property="FontSize"
              Value="32" />
            <Setter Property="FontFamily"
              Value="Consolas" />
            <Setter Property="FontWeight"
              Value="Bold" />
        </Style>
        <Style TargetType="Button"
           BasedOn="{StaticResource commonStyle}">
            <Setter Property="Background"
              Value="#FF17BF26" />
            <Setter Property="Foreground"
              Value= "Black" />
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="C:\סמסטר א- חומר\C#\C#\drone.png"/>
        </Grid.Background>
        <Button Content="Open Drones List" HorizontalAlignment="Center" Height="63" Margin="0,32,0,0" VerticalAlignment="Top" Width="322" Click="Button_Click"/>

    </Grid>
</Window>
